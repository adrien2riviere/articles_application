package com.item.item.model;


import jakarta.persistence.Entity;
import jakarta.persistence.GeneratedValue;
import jakarta.persistence.GenerationType;
import jakarta.persistence.Id;
import jakarta.persistence.Table;
import lombok.Data;

@Entity
@Table(name = "users")
@Data
public class User {

	@Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
	private int id;
	String username;
	String password;
	String langage;
	String timezone;
	String display;
	String sorting;
	String role;
	String department;
	
	public User() {

	}
	
	public User(int id, String username, String password) {
		this.id = id;
		this.username = username;
		this.password = password;
	}
	
	public User(String username, String password, String langage, String timezone, String display, String role, String department) {
		this.username = username;
		this.password = password;
		this.langage =langage;
		this.timezone = timezone;
		this.display = display;
		this.role = role;
		this.department = department;
	}
	
	public void setAttr(String username, String password) {
		this.username = username;
		this.password = password;
	}
	
	public void setUser(String username, String password, String langage, String timezone) {
		this.username = username;
		this.password = password;
		this.langage =langage;
		this.timezone = timezone;
	}
	
	public void setUser(String username, String password, String langage, String timezone, String display, String role) {
		this.username = username;
		this.password = password;
		this.langage =langage;
		this.timezone = timezone;
		this.display = display;
		this.role = role;
	}
	
	public void setBlank() {
		this.username = username.isBlank() ? "" : username;
		this.password = password.isBlank() ? "" : password;
	}
	
	public void trim() {
		this.username = this.username.trim();
	}
	
	public void fill() {
		if(this.role.equals("administrator")) {
			
		}
	}

}
