
var selectRole = document.getElementById("role");
selectRole.addEventListener("select", logSelection);

selectRole
var value = selectRole.value;
if(value == "user") {
	document.getElementById("langage").disabled = false;
}